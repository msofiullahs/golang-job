services:
  goqueue:
    build: .
    ports:
      - "8080:8080"
    environment:
      - USE_REDIS=true
      - REDIS_ADDR=redis:6379
      - DASHBOARD_ADDR=:8080
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - redis_data:/data

volumes:
  redis_data:
